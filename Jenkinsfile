node {
   stage('Preparation') { // for display purposes
        echo "Preparation"
        git 'https://github.com/jianzzz/jenkins-test.git' 
   }
   stage('Build') {
        echo "build"
   }
   stage('Results') {
        echo "Results"
   }
}
